{:register/type :kotoba.security/control-crosswalk
 :register/version 1
 :crosswalk/as-of "2026-08-26"

 ;; ── なぜこの register が要るのか ──────────────────────────────────────────
 ;;
 ;; この repo は NIST CSF 2.0 と SP 800-53 Rev.5 へのマッピングを持っていたが、
 ;; **SOC 2 Trust Services Criteria と ISO/IEC 27001 への対応表を 1 件も持って
 ;; いなかった**（2026-08-23 の grep で 0 件）。そのため「SOC 2 を取れるか」に
 ;; 対して、この repo が既に持っている厚い設計証拠を使って答えることが
 ;; できなかった。ADR-2608231500。
 ;;
 ;; ── 規格本文を複製しない ────────────────────────────────────────────────
 ;;
 ;; AICPA TSC も ISO/IEC 27001 も著作物である。ここが持つのは
 ;;
 ;;   (a) 条項番号（CC6.1 / A.8.5 —— これは事実であり著作物ではない）
 ;;   (b) **私たち自身の言葉による記述子**（規格本文の引用ではない）
 ;;   (c) この repo の evidence-index の claim への写像
 ;;
 ;; だけで、規格の要求事項本文は 1 文も含まない。監査に臨むときは購入した
 ;; 規格原本を正本とし、この表は「私たちの証拠がどこに当たるか」の索引として
 ;; 使う。`:control/descriptor` を規格の要求事項として引用しないこと。
 ;;
 ;; ── design と operating を混ぜない ──────────────────────────────────────
 ;;
 ;; SOC 2 Type I は「ある時点の統制の設計の適正性」、Type II は「一定期間の
 ;; 運用の有効性」に対する意見である。ISO/IEC 27001 の Stage 1 と Stage 2 も
 ;; ほぼ同じ切り方をする。したがって claim には強さの階層があり、
 ;; `:design` の claim を何本積んでも Type II の証拠にはならない。
 ;;
 ;;   :design         設計された・定義された・記録された（-designed / -defined）
 ;;   :implementation 実装され検査で強制されている（-enforced / -executable）
 ;;   :operating      本番で一定期間運用された receipt がある
 ;;
 ;; 2026-08-23 時点で `:operating` の claim は **0 本**である。
 ;; `registers/remaining-operational-gaps.edn` の
 ;; `:operational/readiness :not-qualified` と整合する。

 :crosswalk/claim-strength
 {;; -designed / -defined / -recorded / -seeded は設計まで
  :governance-designed :design
  :ir-designed :design
  :monitoring-designed :design
  :key-lifecycle-designed :design
  :sbom-slsa-designed :design
  :operational-evidence-designed :design
  :fips-non-claim-defined :design
  :architecture-review-recorded :design
  :risk-register-seeded :design
  :kagi-custody-documented :design
  :on-call-runbook :design
  :capability-values-contract :design
  :monitoring-alert-schema :design

  ;; 例・stub・fixture は「動くことを示した」であって「運用した」ではない。
  ;; 名前に -example / -stub / -fixture / -sample / -local が付くものは
  ;; ここで頭打ちにする。これを :implementation に上げない。
  :on-call-roster-example :design
  :monitoring-heartbeat-stub :design
  :pager-local-fixture :design
  :technical-alert-sample :design
  :ir-tabletop-compromised-signer :design

  ;; 実装され、検査が強制している
  :package-admission-gate-enforced :implementation
  :release-evidence-gate-executable :implementation
  :hybrid-admit-ci-gate :implementation
  :crypto-policy-modes-enforceable :implementation
  :crypto-inventory-checked :implementation
  :grant-policy-intersection-pure :implementation
  :capability-intersection-at-host-call-time :implementation
  :capability-receipt-shape :implementation
  :host-call-receipts-in-audit-trail :implementation
  :host-denial-telemetry-aggregate :implementation
  :package-signing-rotation-pure :implementation
  :package-verification :implementation
  :package-verification-receipt-emitted :implementation
  :revoked-signer-denied :implementation
  :envelope-provider-metadata-emitted :implementation
  :hybrid-kem-vectors-real :implementation
  :hybrid-required-object-encryption-consumer :implementation
  :key-lifecycle-rotation-drill :implementation
  :key-status-snapshot :implementation
  :production-public-keys-registered :implementation
  :metric-collect-file-backed :implementation
  :vendor-webhook-adapters :implementation
  :conformance-results :implementation
  :provenance :implementation
  :sbom :implementation
  :risk-review :implementation}

 ;; ⚠ `:pager-wiring` をここに書かないこと。あれは `:evidence/type` であって
 ;; `:evidence/claim` ではない（初版で取り違え、crosswalk_test の
 ;; every-required-claim-exists-in-the-evidence-vocabulary が捕まえた）。
 ;; pager の claim は `:pager-local-fixture`。

 :crosswalk/frameworks
 {:soc2-tsc-2017
  {:label "SOC 2 / AICPA Trust Services Criteria (2017, 2022 revised points of focus)"
   :encoded :common-criteria-only
   :encoded/why "Security（共通規準 CC1〜CC9）だけを符号化した。Availability /
                 Confidentiality / Processing Integrity / Privacy の追加規準は、
                 どのカテゴリを報告書に含めるかが未決なので符号化しない。
                 **未符号化は『該当なし』ではない。**"
   :criteria-encoded 33
   :attestation/type-i :design-suitability
   :attestation/type-ii :operating-effectiveness-over-a-period
   :issued-by "licensed CPA firm"}

  :iso-27001-2022
  {:label "ISO/IEC 27001:2022 + Annex A"
   :encoded :partial-annex-a
   :encoded/why "Annex A は 93 管理策（組織 37 / 人 8 / 物理 14 / 技術 34）。
                 ここに符号化したのは **この repo の証拠が実際に当たる管理策だけ**
                 で、残りは未写像として数える。93 件すべてに記述子を書けば
                 内容の裏付けが無いまま表が埋まり、`:not-yet-mapped` と
                 `:no-evidence` の区別が消える。"
   :annex-a/total 93
   :annex-a/themes {:organizational 37 :people 8 :physical 14 :technological 34}
   :certified-by "accredited certification body (Stage 1 / Stage 2)"}

  :csa-ccm-v4
  {:label "CSA Cloud Controls Matrix v4 / CAIQ"
   :encoded :not-encoded
   :encoded/why "STAR Level 1 は自己申告なので第三者審査を要しないが、CAIQ の
                 197 問に答えるには system description が先に要る。
                 scope 確定（ADR-2608231500）の後に符号化する。"}

  :ismap
  {:label "ISMAP 政府情報システムのためのセキュリティ評価制度"
   :encoded :not-encoded
   :encoded/why "管理基準は ISO/IEC 27001 / 27017 に日本政府固有の要求を重ねた
                 もので、監査機関による監査が要る。`90-docs/regulatory/permits.datoms.edn`
                 に **ガバメントクラウド申請の前提条件** として既に登録済みだが、
                 自社取得の対象としては未着手。政府調達を狙う判断が先。"}}

 ;; ── 統制 ──────────────────────────────────────────────────────────────
 ;;
 ;; `:control/requires-claims` は「この規準を満たしたと言うために、この repo の
 ;; evidence-index に在ってほしい claim」。空集合は **未写像**であって
 ;; 「証拠不要」ではない。`:control/gap` に理由を書く。

 :crosswalk/controls
 [;; ── CC1 統制環境 ────────────────────────────────────────────────────
  {:control/framework :soc2-tsc-2017 :control/id "CC1.1"
   :control/descriptor "誠実性と倫理観に対する組織の約束"
   :control/csf [:govern] :control/nist-800-53 [:PM]
   :control/requires-claims #{}
   :control/gap "行動規範・その受領記録・違反時の手続が無い。1 名体制でも文書は要る。"}
  {:control/framework :soc2-tsc-2017 :control/id "CC1.2"
   :control/descriptor "統治機関による監督責任"
   :control/csf [:govern] :control/nist-800-53 [:PM]
   :control/requires-claims #{:governance-designed}
   :control/gap "docs/governance.md が役割を定義しているが、**実在の人名が
                 割り当てられておらず、レビューが行われた記録も無い**。"}
  {:control/framework :soc2-tsc-2017 :control/id "CC1.3"
   :control/descriptor "組織構造・報告経路・権限と責任の割当"
   :control/csf [:govern] :control/nist-800-53 [:PM]
   :control/requires-claims #{:governance-designed}
   :control/gap "役割は 6 つ定義済み。実在の保有者への割当が未了。"}
  {:control/framework :soc2-tsc-2017 :control/id "CC1.4"
   :control/descriptor "適格な人員の採用・育成・維持"
   :control/csf [:govern] :control/nist-800-53 [:AT :PS]
   :control/requires-claims #{}
   :control/gap "採用時の身元確認・秘密保持契約・セキュリティ教育の記録が無い
                 （standards-assessment.md が AT 系を『訓練の成果物が無い』と自認）。"}
  {:control/framework :soc2-tsc-2017 :control/id "CC1.5"
   :control/descriptor "内部統制責任に対する個人の説明責任"
   :control/csf [:govern] :control/nist-800-53 [:PM :PS]
   :control/requires-claims #{:governance-designed}
   :control/gap "「全てのセキュリティ決定は受諾した役割を名指しする」と規定
                 されているが、名指しされた実例が無い。"}

  ;; ── CC2 情報と伝達 ──────────────────────────────────────────────────
  {:control/framework :soc2-tsc-2017 :control/id "CC2.1"
   :control/descriptor "内部統制を機能させるための関連性のある質の高い情報の利用"
   :control/csf [:identify] :control/nist-800-53 [:AU :CM]
   :control/requires-claims #{:host-call-receipts-in-audit-trail :capability-receipt-shape}
   :control/gap nil}
  {:control/framework :soc2-tsc-2017 :control/id "CC2.2"
   :control/descriptor "内部統制に関する責任の内部への伝達"
   :control/csf [:govern] :control/nist-800-53 [:AT :PM]
   :control/requires-claims #{:governance-designed :on-call-runbook}
   :control/gap "伝達の受領記録が無い。"}
  {:control/framework :soc2-tsc-2017 :control/id "CC2.3"
   :control/descriptor "外部関係者との伝達"
   :control/csf [:govern] :control/nist-800-53 [:IR :SA]
   :control/requires-claims #{}
   :control/gap "SECURITY.md はあるが、顧客・subprocessor への通知経路と
                 その実行記録が無い。"}

  ;; ── CC3 リスク評価 ──────────────────────────────────────────────────
  {:control/framework :soc2-tsc-2017 :control/id "CC3.1"
   :control/descriptor "目的の明確化によるリスクの識別と評価"
   :control/csf [:identify] :control/nist-800-53 [:RA :PM]
   :control/requires-claims #{:risk-register-seeded}
   :control/gap "risk register は seeded であって operated ではない。"}
  {:control/framework :soc2-tsc-2017 :control/id "CC3.2"
   :control/descriptor "目的達成に対するリスクの識別と分析（資産の識別を含む）"
   :control/csf [:identify] :control/nist-800-53 [:RA :CM]
   :control/requires-claims #{:risk-register-seeded :risk-review}
   :control/gap "資産台帳は 2026-08-23 に superproject 側で機械生成できるように
                 なった（90-docs/compliance/scope.datoms.edn）。**データ分類が未了**。"}
  {:control/framework :soc2-tsc-2017 :control/id "CC3.3"
   :control/descriptor "不正リスクの考慮"
   :control/csf [:identify] :control/nist-800-53 [:RA]
   :control/requires-claims #{}
   :control/gap "risk register に insider / fraud のカテゴリが無い。"}
  {:control/framework :soc2-tsc-2017 :control/id "CC3.4"
   :control/descriptor "内部統制に重大な影響を与えうる変化の識別と評価"
   :control/csf [:identify] :control/nist-800-53 [:RA :CM]
   :control/requires-claims #{:risk-review}
   :control/gap nil}

  ;; ── CC4 モニタリング活動 ────────────────────────────────────────────
  {:control/framework :soc2-tsc-2017 :control/id "CC4.1"
   :control/descriptor "内部統制の構成要素が機能していることの継続的・個別的評価"
   :control/csf [:detect] :control/nist-800-53 [:CA :SI]
   :control/requires-claims #{:monitoring-designed :metric-collect-file-backed}
   :control/gap "live telemetry が無い（remaining-operational-gaps W9）。
                 監視は設計と file-backed の収集器まで。"}
  {:control/framework :soc2-tsc-2017 :control/id "CC4.2"
   :control/descriptor "内部統制の不備の評価と是正責任者への伝達"
   :control/csf [:detect] :control/nist-800-53 [:CA :IR]
   :control/requires-claims #{:architecture-review-recorded}
   :control/gap "architecture-review-findings.edn に所見はあるが、
                 是正の完了記録と再評価が無い。"}

  ;; ── CC5 統制活動 ────────────────────────────────────────────────────
  {:control/framework :soc2-tsc-2017 :control/id "CC5.1"
   :control/descriptor "リスクを許容水準まで低減する統制活動の選定と整備"
   :control/csf [:protect] :control/nist-800-53 [:CM :SC]
   :control/requires-claims #{:grant-policy-intersection-pure
                              :capability-intersection-at-host-call-time}
   :control/gap nil}
  {:control/framework :soc2-tsc-2017 :control/id "CC5.2"
   :control/descriptor "技術に対する全般統制の選定と整備"
   :control/csf [:protect] :control/nist-800-53 [:CM :SC :SI]
   :control/requires-claims #{:package-admission-gate-enforced
                              :release-evidence-gate-executable}
   :control/gap nil}
  {:control/framework :soc2-tsc-2017 :control/id "CC5.3"
   :control/descriptor "方針と手順による統制活動の展開"
   :control/csf [:govern] :control/nist-800-53 [:PL :PM]
   :control/requires-claims #{:governance-designed}
   :control/gap "方針はあるが、承認・配布・年次レビューの記録が無い。"}

  ;; ── CC6 論理・物理アクセス統制 ──────────────────────────────────────
  {:control/framework :soc2-tsc-2017 :control/id "CC6.1"
   :control/descriptor "論理アクセスのためのセキュリティソフトウェア・インフラ・
                        アーキテクチャの整備"
   :control/csf [:protect] :control/nist-800-53 [:AC :IA :SC]
   :control/requires-claims #{:capability-intersection-at-host-call-time
                              :grant-policy-intersection-pure
                              :capability-values-contract}
   :control/gap nil}
  {:control/framework :soc2-tsc-2017 :control/id "CC6.2"
   :control/descriptor "利用者登録・変更・アクセス権付与の承認"
   :control/csf [:protect] :control/nist-800-53 [:AC :IA]
   :control/requires-claims #{}
   :control/gap "**人**の利用者登録／退職時の権限剥奪の手続と記録が無い。
                 capability モデルは機械の権限であって人の権限ではない。"}
  {:control/framework :soc2-tsc-2017 :control/id "CC6.3"
   :control/descriptor "アクセス権の付与・変更・削除の管理と最小権限"
   :control/csf [:protect] :control/nist-800-53 [:AC]
   :control/requires-claims #{:grant-policy-intersection-pure}
   :control/gap "人の側のアクセスレビュー（四半期棚卸し）が無い。"}
  {:control/framework :soc2-tsc-2017 :control/id "CC6.4"
   :control/descriptor "物理的アクセスの制限"
   :control/csf [:protect] :control/nist-800-53 [:PE]
   :control/requires-claims #{}
   :control/gap "murakumo fleet は物理的な mac-mini 群。**設置場所の
                 物理的アクセス統制が文書化されていない**。standards-assessment.md
                 は PE を『out of scope』としているが、自社ハードウェアが
                 本番を担う以上スコープ外にはできない。"}
  {:control/framework :soc2-tsc-2017 :control/id "CC6.5"
   :control/descriptor "資産の廃棄時の情報の論理的・物理的な抹消"
   :control/csf [:protect] :control/nist-800-53 [:MP]
   :control/requires-claims #{}
   :control/gap "媒体廃棄手順が無い。"}
  {:control/framework :soc2-tsc-2017 :control/id "CC6.6"
   :control/descriptor "システム境界の外部からの脅威に対する論理アクセス統制"
   :control/csf [:protect] :control/nist-800-53 [:AC :SC]
   :control/requires-claims #{:capability-intersection-at-host-call-time
                              :host-denial-telemetry-aggregate}
   :control/gap nil}
  {:control/framework :soc2-tsc-2017 :control/id "CC6.7"
   :control/descriptor "情報の伝送・移動・除去の制限と保護"
   :control/csf [:protect] :control/nist-800-53 [:SC :MP]
   :control/requires-claims #{:hybrid-required-object-encryption-consumer
                              :envelope-provider-metadata-emitted}
   :control/gap nil}
  {:control/framework :soc2-tsc-2017 :control/id "CC6.8"
   :control/descriptor "許可されていないソフトウェアの導入・実行の防止"
   :control/csf [:protect] :control/nist-800-53 [:CM :SI :SR]
   :control/requires-claims #{:package-admission-gate-enforced
                              :package-verification
                              :revoked-signer-denied}
   :control/gap nil}

  ;; ── CC7 システム運用 ────────────────────────────────────────────────
  {:control/framework :soc2-tsc-2017 :control/id "CC7.1"
   :control/descriptor "構成の逸脱と新たな脆弱性の検知手段の整備"
   :control/csf [:detect] :control/nist-800-53 [:CM :RA :SI]
   :control/requires-claims #{:crypto-inventory-checked}
   :control/gap "SBOM と CVE 照合の **モデルは存在する** —— `kotoba-lang/app-sbom`
                 が component(purl) / OSV からの CVE 取り込み / VulnMatch /
                 severity 別 SLA / blast radius を 1,823 行 + テストで持つ。
                 欠けているのは 2 つ: (1) `sbom.etzhayyim.com` が名前解決せず
                 **未デプロイ**、(2) このワークスペースの実依存を component として
                 投入するものが無い。つまり『仕組みが無い』のではなく
                 **自分たちの依存を一度も測っていない**。初版のこの gap 欄は
                 『脆弱性スキャンが 1 本も存在しない』と書いていたが誤りで、
                 索引を引かずに不在を結論した（2026-08-23 訂正）。"}
  {:control/framework :soc2-tsc-2017 :control/id "CC7.2"
   :control/descriptor "異常・セキュリティ事象の監視と分析"
   :control/csf [:detect] :control/nist-800-53 [:AU :SI :IR]
   :control/requires-claims #{:monitoring-alert-schema :host-denial-telemetry-aggregate
                              :technical-alert-sample}
   :control/gap "アラートは schema と sample まで。本番の発火記録が無い。"}
  {:control/framework :soc2-tsc-2017 :control/id "CC7.3"
   :control/descriptor "セキュリティ事象の評価とインシデント該当性の判断"
   :control/csf [:respond] :control/nist-800-53 [:IR]
   :control/requires-claims #{:ir-designed :ir-tabletop-compromised-signer}
   :control/gap "机上演習 1 回。実インシデントの記録が無い（これは良いことだが、
                 Type II では期間中の運用の証拠が要る）。"}
  {:control/framework :soc2-tsc-2017 :control/id "CC7.4"
   :control/descriptor "識別されたインシデントへの対応"
   :control/csf [:respond] :control/nist-800-53 [:IR]
   :control/requires-claims #{:ir-designed :pager-local-fixture :on-call-runbook}
   :control/gap "human on-call が居ない（remaining-operational-gaps W9）。
                 pager は local fixture まで。"}
  {:control/framework :soc2-tsc-2017 :control/id "CC7.5"
   :control/descriptor "識別されたインシデントからの復旧"
   :control/csf [:recover] :control/nist-800-53 [:CP :IR]
   :control/requires-claims #{}
   :control/gap "destructive-restore-drill register は在るが receipt が無い
                 （W5 の :destructive-restore-receipt が missing）。"}

  ;; ── CC8 変更管理 ────────────────────────────────────────────────────
  {:control/framework :soc2-tsc-2017 :control/id "CC8.1"
   :control/descriptor "インフラ・データ・ソフトウェア・手順の変更の authorize /
                        design / develop / test / approve / implement"
   :control/csf [:protect] :control/nist-800-53 [:CM :SA]
   :control/requires-claims #{:release-evidence-gate-executable :provenance :sbom}
   :control/gap "**この workspace で最も脆い規準。** GitHub Actions は org 単位で
                 停止済みで、CI の正本は murakumo fleet に移った。ところが
                 CLAUDE.md 自身の実測で、fleet gate のうち 6 本は landing 以来
                 一度も緑になっていない。『変更は必ず検査を通る』を期間分の
                 証拠で示す経路が今は無い。"}

  ;; ── CC9 リスク軽減 ──────────────────────────────────────────────────
  {:control/framework :soc2-tsc-2017 :control/id "CC9.1"
   :control/descriptor "事業中断リスクの軽減活動の識別・選定・展開"
   :control/csf [:recover] :control/nist-800-53 [:CP :RA]
   :control/requires-claims #{}
   :control/gap "BCP / DR の目標（RTO / RPO）が無い。"}
  {:control/framework :soc2-tsc-2017 :control/id "CC9.2"
   :control/descriptor "vendor / business partner に関連するリスクの評価と管理"
   :control/csf [:govern] :control/nist-800-53 [:SR :SA]
   :control/requires-claims #{:vendor-webhook-adapters}
   :control/gap "subprocessor は 2026-08-23 に機械的に列挙できるようになった
                 （cloudflare / backblaze-b2 / ipfs.io / murakumo-fleet ほか）。
                 **各社のデューデリジェンス記録と契約上の保護条項が未整備。**"}

  ;; ── ISO/IEC 27001:2022 Annex A（証拠が実際に当たる管理策のみ） ──────
  {:control/framework :iso-27001-2022 :control/id "A.5.9"
   :control/descriptor "情報および関連資産の目録"
   :control/csf [:identify]
   :control/requires-claims #{}
   :control/gap "資産目録は superproject の 90-docs/compliance/scope.datoms.edn
                 として機械生成できるようになった。**この repo の
                 evidence-index にはまだ claim として入っていない。**"}
  {:control/framework :iso-27001-2022 :control/id "A.5.15"
   :control/descriptor "アクセス制御"
   :control/csf [:protect]
   :control/requires-claims #{:grant-policy-intersection-pure
                              :capability-intersection-at-host-call-time}
   :control/gap nil}
  {:control/framework :iso-27001-2022 :control/id "A.5.17"
   :control/descriptor "認証情報"
   :control/csf [:protect]
   :control/requires-claims #{:kagi-custody-documented :key-status-snapshot}
   :control/gap "kagi の保管は文書化済み。人の資格情報のライフサイクルが未整備。"}
  {:control/framework :iso-27001-2022 :control/id "A.5.19"
   :control/descriptor "供給者関係における情報セキュリティ"
   :control/csf [:govern]
   :control/requires-claims #{}
   :control/gap "CC9.2 と同じ穴。"}
  {:control/framework :iso-27001-2022 :control/id "A.5.23"
   :control/descriptor "クラウドサービスの利用における情報セキュリティ"
   :control/csf [:govern]
   :control/requires-claims #{}
   :control/gap "Cloudflare / Backblaze の設定基準と責任分界の文書が無い。
                 ISO/IEC 27017 を併せて取るならここが中心。"}
  {:control/framework :iso-27001-2022 :control/id "A.5.24"
   :control/descriptor "情報セキュリティインシデント管理の計画および準備"
   :control/csf [:respond]
   :control/requires-claims #{:ir-designed}
   :control/gap nil}
  {:control/framework :iso-27001-2022 :control/id "A.5.30"
   :control/descriptor "事業継続のための ICT の備え"
   :control/csf [:recover]
   :control/requires-claims #{}
   :control/gap "CC9.1 と同じ穴。"}
  ;; A.8.7 was absent from this register until 2026-08-26. The reason it was
  ;; absent is worth keeping: CC6.8 (prevention of unauthorised software
  ;; execution) is mapped here to the package admission gate and signature
  ;; verification, and that mapping is correct — it is genuinely how this
  ;; workspace answers "how do you stop hostile code running". Having answered
  ;; that once, nobody noticed that A.8.7 asks a wider question: detection,
  ;; prevention, **recovery**, and user awareness. An admission gate is one
  ;; quarter of that, and a register that omits the control cannot show which
  ;; three quarters are missing.
  {:control/framework :iso-27001-2022 :control/id "A.8.7"
   :control/descriptor "マルウェアからの保護（検出・防止・復旧・利用者の認識）"
   :control/csf [:protect :detect :recover]
   :control/nist-800-53 [:SI :CM :AT]
   :control/requires-claims #{}
   :control/gap "**防止**は CC6.8 と同じ証拠が当たる（admission gate + 署名検証 +
                 失効署名者の拒否）。残る 3 つは未達で、しかも別々の理由で未達である。
                 **検出**: 2026-08-26 に `cloud-itonami/aratame`（ADR-2608262300）が
                 判定面として着地した —— 証拠から verdict を出し、独立した governor が
                 admission を決める。ただし検出エンジンは持たず、常駐監視も content
                 inspection も無い。実機の検出は macOS の XProtect が担っており、
                 `aratame doctor` はその版を報告するが、**版を報告することと
                 統制を運用することは別**である。
                 **復旧**: `kotoba-lang/quarantine` が可逆隔離（delete opcode を持たない）
                 と restore を実装しているが、マルウェア事象からの復旧手順として
                 演習された receipt は無い。
                 **利用者の認識**: 対応する仕組みが 1 つも無い（教育・周知の register が
                 存在しない）。A.5.30 / W 系 workstream と同じ外部運用待ちですらない。
                 したがって `:requires-claims` は空のままにする —— ここに CC6.8 の
                 claim を写すと、4 分の 1 の証拠で統制が満たされたように見える。
                 evidenced ではなく **not-mapped** が今日の正しい状態である。"}
  {:control/framework :iso-27001-2022 :control/id "A.8.8"
   :control/descriptor "技術的脆弱性の管理"
   :control/csf [:identify :protect]
   :control/requires-claims #{}
   :control/gap "CC7.1 と同じ穴。`kotoba-lang/app-sbom` にモデルは在るが未デプロイで、
                 自分たちの依存が 1 件も component として登録されていない。
                 A.8.8 が問うのは仕組みの有無ではなく **脆弱性を実際に把握し
                 対処しているか** なので、モデルの存在は答えにならない。"}
  {:control/framework :iso-27001-2022 :control/id "A.8.9"
   :control/descriptor "構成管理"
   :control/csf [:protect]
   :control/requires-claims #{:package-admission-gate-enforced :provenance}
   :control/gap nil}
  {:control/framework :iso-27001-2022 :control/id "A.8.15"
   :control/descriptor "ログ取得"
   :control/csf [:detect]
   :control/requires-claims #{:host-call-receipts-in-audit-trail
                              :host-denial-telemetry-aggregate}
   :control/gap nil}
  {:control/framework :iso-27001-2022 :control/id "A.8.24"
   :control/descriptor "暗号の利用"
   :control/csf [:protect]
   :control/requires-claims #{:crypto-policy-modes-enforceable :crypto-inventory-checked
                              :hybrid-admit-ci-gate}
   :control/gap nil}
  {:control/framework :iso-27001-2022 :control/id "A.8.25"
   :control/descriptor "セキュリティに配慮した開発のライフサイクル"
   :control/csf [:protect]
   :control/requires-claims #{:release-evidence-gate-executable :conformance-results}
   :control/gap nil}
  {:control/framework :iso-27001-2022 :control/id "A.8.32"
   :control/descriptor "変更管理"
   :control/csf [:protect]
   :control/requires-claims #{:release-evidence-gate-executable :provenance}
   :control/gap "CC8.1 と同じ穴。fleet gate の緑が期間分そろっていない。"}]}
