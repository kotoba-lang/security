{:drill/id "key-lifecycle-revoke-drill"
 :drill/date "2026-07-17"
 :drill/type :key-lifecycle-drill
 :drill/scope :pure-synthetic
 :drill/result :pass
 :drill/live-register-mutated? false
 :drill/notes
 "Honest automated pure drill only — NOT a production incident.
  Synthetic two-key package-signing register was built in-memory.
  One key was revoked with reason drill-2026-07-17; blocked-signer-ids
  and evaluate-key-register rejected the revoked signer while the other
  remained :active (evaluate still ok?). Revoking the last active key
  yielded :no-active-signing-key (ok? false). Package-admission trust
  fold was unit-tested as set-union of blocked-signer-ids into
  :revoked-signers (same fold used by kotoba package admission
  --key-register). Live registers/key-register.edn demo-active keys
  were NOT left revoked and were not mutated by this drill."
 :drill/steps
 [{:step 1
   :action :build-mini-register
   :keys ["drill-pkg-a" "drill-pkg-b"]
   :status-start :active
   :public-material :test-local-fake-placeholder
   :private-keys? false}
  {:step 2
   :action :revoke-key
   :target "drill-pkg-a"
   :reason "drill-2026-07-17"
   :at "2026-07-17"
   :assert [:status-transition-ok?
            :revoked-in-blocked-signer-ids
            :remaining-key-active
            :evaluate-key-register-ok?]}
  {:step 3
   :action :package-admission-trust-fold
   :fold :set-union-blocked-into-revoked-signers
   :assert [:revoked-signer-present-in-trust
            :active-signer-absent-from-trust]}
  {:step 4
   :action :revoke-last-active
   :target "drill-pkg-only"
   :assert [:evaluate-not-ok?
            :problem-no-active-signing-key]}]
 :drill/artifacts
 [{:kind :test :path "test/kotoba/security/key_lifecycle_drill_test.clj"}
  {:kind :gate :path "scripts/check-key-lifecycle-drill.cljs"}
  {:kind :code :path "src/kotoba/security/key_status.cljc"}]
 :drill/related-risks ["R-002"]
 :drill/related-evidence ["EV-0009"]}
