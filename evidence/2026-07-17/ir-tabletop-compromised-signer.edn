{:drill/id "ir-tabletop-compromised-signer"
 :drill/date "2026-07-17"
 :drill/type :ir-tabletop
 :drill/scenario "compromised-package-signer"
 :drill/scope :tabletop-simulated
 :drill/result :pass
 :drill/live-register-mutated? false
 :drill/honesty
 "SIMULATED tabletop only — not a production incident, not a live key compromise.
  No private keys were generated, exported, or committed. No production systems
  were frozen. Outcomes below are expected control behaviors under the written
  playbooks, exercised verbally / on paper with pure register helpers as props."
 :drill/related-risks ["R-005" "R-002"]
 :drill/related-playbook "docs/incident-response.md#compromised-package-signer"
 :drill/roles
 [{:role :incident-commander
   :owner :operations-owner
   :duties ["declare SEV-1" "freeze release train" "own timeline"]}
  {:role :crypto-owner
   :owner :crypto-owner
   :duties ["mark key :compromised/:revoked in register" "issue replacement public material"]}
  {:role :package-owner
   :owner :package-owner
   :duties ["enumerate locks/manifests using the signer" "publish replacement locks"]}
  {:role :security-owner
   :owner :security-owner
   :duties ["evidence-index entry" "advisory draft" "postmortem linkage"]}]
 :drill/steps
 [{:step 1
   :action :detect-and-triage
   :inputs ["suspicious package signature" "signer id" "run receipt"]
   :expected ["severity SEV-1" "preserve locks/manifests/receipts by CID or digest"]}
  {:step 2
   :action :contain
   :expected ["freeze releases" "disable signing path that uses the suspect key"
              "do not rewrite audit logs"]}
  {:step 3
   :action :revoke-in-register
   :expected ["key status -> :compromised then :revoked (pure map)"
              "reason + effective time recorded"
              "live demo keys NOT left revoked in this simulated drill"]}
  {:step 4
   :action :reject-new-artifacts
   :expected ["package verify --key-register folds revoked id into :revoked-signers"
              "new locks signed only by that key -> :package/signer-not-trusted"]}
  {:step 5
   :action :replace-and-recover
   :expected ["new public key registered :pre-active"
              "promote-to-active after oob secret provision"
              "replacement locks published"
              "check-key-register --require-active green for regulated path"]}
  {:step 6
   :action :close
   :expected ["root cause documented" "evidence-index entry" "advisory linked"
              "regression fixture/test referenced"]}]
 :drill/expected-outcomes
 ["SEV-1 freeze decision made within tabletop timebox"
  "Register-only revocation path is clear (no private key in-repo dependency)"
  "Consumers understand --key-register fold into signer-not-trusted"
  "Gaps logged: live continuous monitoring still incomplete (R-005 residual)"]
 :drill/artifacts
 [{:kind :playbook :path "docs/incident-response.md"}
  {:kind :runbook :path "docs/key-ops-production.md"}
  {:kind :code :path "src/kotoba/security/key_status.cljc"}
  {:kind :drill :path "evidence/2026-07-17/ir-technical-key-revoke-drill.edn"}]}
