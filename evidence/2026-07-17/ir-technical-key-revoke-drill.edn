{:drill/id "ir-technical-key-revoke-drill"
 :drill/date "2026-07-17"
 :drill/type :ir-technical
 :drill/scenario "key-revoke-pure-lifecycle"
 :drill/scope :pure-synthetic
 :drill/result :pass
 :drill/live-register-mutated? false
 :drill/honesty
 "Technical IR drill using pure key_status helpers only. Synthetic two-key
  package-signing register in-memory. Live registers/key-register.edn demo
  keys were NOT mutated or left revoked. No private keys involved.
  Package-admission fold asserted as set-union of blocked-signer-ids into
  trust :revoked-signers (same fold as kotoba package verify --key-register)."
 :drill/related-risks ["R-005" "R-002"]
 :drill/aliases ["key-lifecycle-revoke-drill"]
 :drill/steps
 [{:step 1
   :action :build-mini-register
   :keys ["drill-pkg-a" "drill-pkg-b"]
   :status-start :active
   :public-material :test-local-fake-placeholder
   :private-keys? false}
  {:step 2
   :action :revoke-key
   :target "drill-pkg-a"
   :reason "drill-2026-07-17"
   :at "2026-07-17"
   :assert [:status-transition-ok?
            :revoked-in-blocked-signer-ids
            :remaining-key-active
            :evaluate-key-register-ok?]}
  {:step 3
   :action :package-admission-trust-fold
   :fold :set-union-blocked-into-revoked-signers
   :assert [:revoked-signer-present-in-trust
            :active-signer-absent-from-trust]
   :expected-package-admission
   "verify-lock / admit with --key-register containing revoked signer id matching
    dep/signers yields :package/signer-not-trusted (cross-repo E2E in kotoba)."}
  {:step 4
   :action :revoke-last-active
   :target "drill-pkg-only"
   :assert [:evaluate-not-ok?
            :problem-no-active-signing-key]}
  {:step 5
   :action :rotation-helpers
   :assert [:retire-key-blocks-new
            :rotate-signing-key-promotes-new-retires-old]
   :note "Covered by key_status_test retire-key + rotate-signing-key unit tests."}]
 :drill/gates
 ["nbb --classpath src scripts/check-key-lifecycle-drill.cljs"
  "clojure -M:test (key_lifecycle_drill_test + key_status_test)"]
 :drill/artifacts
 [{:kind :drill :path "evidence/2026-07-17/key-lifecycle-revoke-drill.edn"}
  {:kind :test :path "test/kotoba/security/key_lifecycle_drill_test.clj"}
  {:kind :test :path "test/kotoba/security/key_status_test.clj"}
  {:kind :gate :path "scripts/check-key-lifecycle-drill.cljs"}
  {:kind :code :path "src/kotoba/security/key_status.cljc"}
  {:kind :runbook :path "docs/key-ops-production.md"}]
 :drill/related-evidence ["EV-0010" "EV-0011"]}
